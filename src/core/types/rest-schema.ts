export type RestSchema = {
  PORT: number;
  SALT: string;
  DB_USER: string,
  DB_PASSWORD: string,
  DB_HOST: string,
  DB_PORT: string,
  UPLOAD_DIRECTORY: string,
  JWT_ACCESS_SECRET: string,
  JWT_REFRESH_SECRET: string
};
